<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <snap class="title"><%= @post.title %></span>
</p>

<p>
  <strong>Content:</strong>
  <snap class="content"><%= @post.content %></span>
</p>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
